apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: post-api
type: Opaque
stringData:
  # Copy this file to secret.yaml and replace the placeholder.
  # secret.yaml is gitignored — never commit the real password.
  # Jenkins CD creates this Secret automatically from the
  # 'db-password' credential.
  password: REPLACE_ME
